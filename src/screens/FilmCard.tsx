import React from 'react';
import {Text, View} from 'react-native';
import {Props} from '../App';

const FilmCard = ({navigation,route}:Props) => {
  return (
    <View>
      <Text>FilmScreen</Text>
    </View>
  );
};

export default FilmCard;
